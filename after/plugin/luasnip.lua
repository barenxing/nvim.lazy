-- luasnip.lua

local ls = require("luasnip")
local s = ls.snippet
local t = ls.text_node
local i = ls.insert_node
local f = ls.function_node

-- Function to return the date string
local function today()
  return os.date("%Y-%m-%d")
end

ls.add_snippets("python", {
  s("hearder", {
    t("# "),
    i(1),
    t({ "", "#", "" }),
    t("# Created by Richard Hu on "),
    f(today, {}),
    t({ "", "#", "" }),
    i(2),
  }),
  s("mltemplate", {
    t({
      "# import libraries",
      "import pandas as pd",
      "import numpy as np",
      "import matplotlib.pyplot as plt",
      "from sklearn.model_selection import train_test_split",
      "from sklearn.preprocessing import StandardScaler",
      "",
      "",
      "# Set print option to up to 2 decimal points",
      "# np.set_printoptions(precision=2, suppress=True)",
      "#",
      "# or use formatter to force print exactly 2 decimal points always",
      "# np.set_printoptions(formatter={'float': '{: 0.2f}'.format})",
      "",
      "",
      "# Load data",
      "df = pd.read_csv('data.csv', delimiter=',')",
      "print(df.head(5))",
      "",
      "",
      "# Split data into features and target",
      "X = df.iloc[:, :-1]",
      "y = df.iloc[:, -1]",
      "",
      "# X = df.drop('target', axis=1)",
      "# y = df['target']",
      "",
      "print(X.head(5))",
      "print(y.head(5))",
      "",
      "",
      "#",
      "# Encode categorical data",
      "#",
      "",
      "",
      "# Split the dataset into training and testing sets",
      "(X_train, X_test, y_train, y_test) = train_test_split(X, y, test_size=0.2, random_state=0)",
      "",
      "# Create an instance of StandardScaler",
      "sc = StandardScaler()",
      "",
      "# Fit the scaler to the training data and transform both the training and testing data",
      "X_train_scaled = sc.fit_transform(X_train)",
      "X_test_scaled = sc.transform(X_test)",
      "",
    }),
  }),
  s("one_hot_encoder", {
    t({
      "from sklearn.compose import ColumnTransformer",
      "from sklearn.preprocessing import OneHotEncoder, LabelEncoder",
      "",
      "# Identify categorical features",
      "categorical_features = ['feature1', 'feature2']  # Replace with your actual categorical feature names",
      "",
      "# Implement an instance of the ColumnTransformer",
      "ct = ColumnTransformer(transformers=[('encoder', OneHotEncoder(), categorical_features)], remainder='passthrough')",
      "",
      "# Apply the fit_transform method to the features",
      "X = np.array(ct.fit_transform(X))",
      "",
      "# Use LabelEncoder to encode binary categorical data",
      "le = LabelEncoder()",
      "y = le.fit_transform(y)",
    }),
  }),
  s("piplot1", {
    t({
      "import matplotlib.pyplot as plt",
      "",
      "# Create a figure and axis",
      "fig, ax = plt.subplots()",
      "",
      "# Plot data",
      "ax.scatter(X, y, color='red')",
      "ax.plot(X, regressor.predict(X), color='blue')",
      "",
      "# Set title and label",
      "ax.set_title('Plot_Title')",
      "ax.set_xlabel('X-axis_label')",
      "ax.set_ylabel('Y-axis_label')",
      "",
      "# Show grid, optional",
      "ax.grid()",
      "",
      "# Display the plot",
      "plt.show()",
    }),
  }),
  s("piplot2", {
    t({
      "import matplotlib.pyplot as plt",
      "",
      "# plot the training set results",
      "fig, ax = plt.subplots(1, 2, figsize=(12, 5))",
      "",
      "# Training set plot (left)",
      "ax[0].scatter(X_train, y_train, color='red')",
      "ax[0].plot(X_train, regressor.predict(X_train), color='blue')",
      "ax[0].set_title('Chart_Title')",
      "ax[0].set_xlabel('X_label')",
      "ax[0].set_ylabel('y_label')",
      "ax[0].grid()",
      "",
      "# Test set plot (right)",
      "ax[1].scatter(X_test, y_test, color='green')",
      "ax[1].plot(X_train, regressor.predict(X_train), color='blue')  # Use training fit for regression line",
      "ax[1].set_title('Plot_Title')",
      "ax[1].set_xlabel('x-axis_label')",
      "ax[1].set_ylabel('y-axis_label')",
      "ax[1].grid()",
      "",
      "# Display the plot",
      "plt.tight_layout()",
      "plt.show()",
    }),
  }),
})
